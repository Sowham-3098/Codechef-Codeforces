#include<stdio.h>

int main(){
    int A;
    scanf("%d", &A);
    int arr[A];
     
        for (int i = 0; i < A; ++i)
        {
            scanf("%d", &arr[A]);
        }
         
    return 0;
}